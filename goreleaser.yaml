# .goreleaser.yml
builds:
  - id: "mybuild"
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - 386
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
